# Example only — do NOT put real secrets here.
# These are safe placeholders; real values should come from runtime env or a secrets manager.
MLFLOW_TRACKING_URI=http://localhost:5000
MLFLOW_S3_ENDPOINT_URL=http://minio.local:9000
MLFLOW_S3_BUCKET=northstrike-artifacts
